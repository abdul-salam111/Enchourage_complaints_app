name: create_feature
description: Create a new feature with MVC/MVVM/Clean architecture
version: 0.1.0+1

vars:
  page_name:
    type: string
    description: What is the name of your feature?
    default: signin
    prompt: Enter feature name (e.g., signin, home, profile)
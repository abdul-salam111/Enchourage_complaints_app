name: create_page
description: Create a new page with MVC pattern
version: 0.1.0+1

vars:
  page_name:
    type: string
    description: What is the name of your page?
    default: signin
    prompt: Enter page name (e.g., signin, home, profile)
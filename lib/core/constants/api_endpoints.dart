class ApiEndPoints {
  static const baseUrl = "https://anchorageislamabad.com/api/";
  static const String loginEndpoint = "${baseUrl}login";
  static const String allComplaintsEndpoint = "${baseUrl}login";
}

import '../../../../app_exports.dart';

class AddBillTab extends StatelessWidget {
  const AddBillTab({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Add Bill Content', style: context.bodyMedium));
  }
}
